# PROJECT BRIEF - READ BEFORE EVERY TASK

**Competition:** Nansen CLI Build Challenge (#NansenCLI)
**Deadline:** Mar 22, 11:59 PM SGT
**Rules:** minimum 10 Nansen CLI API calls
**Data sources:** NANSEN CLI ONLY
**x402 wallet:** 0xfCb580143CeE50f51dC01972e446444a63862A4a (Base)
**Budget remaining:** ~$1.35 USDC — MAX 2 more full test runs
**API key:** REDACTED (0 credits, use x402)
**Judging:** creativity, usefulness, technical depth, presentation on X
**x402 NOTE:** Must remove API key for x402 to work. Run `nansen logout` before API calls. API key + 0 credits = CREDITS_EXHAUSTED error instead of x402 fallback.

## Priority Order (competition impact)

### P0 — Must-have to win
1. "Why it matters" + "What to watch" explainers per section — #1 differentiator
2. Actionable summary at top (bull case / bear case / key questions)
3. Fix empty charts — show real data or clean "no data" state
4. Price chart with timeframe context (use OHLCV data already fetched)

### P1 — Strong polish
5. Clickable addresses → Solscan/Etherscan links
6. Replace "SM" with "Smart Money" everywhere
7. Risk indicators with reasoning, not just "MEDIUM"
8. Holder identity labels

### P2 — Nice to have
9. SM Holdings context label
10. Webapp feel improvements

## CONSTRAINTS
- Do NOT run API calls until all template/UI fixes are done
- Use JUP cached data for development
- Only 1 clean final run on a trending token for submission screenshots
- That's the budget. No waste.
